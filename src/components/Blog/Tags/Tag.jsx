import PropTypes from "prop-types";
const Tag = ({tag}) => {
    return (
        <div>
            
        </div>
    );
};
Tag.propTypes = {
    tag: PropTypes.object.isRequired,
  };
export default Tag;